# 步骤1：确认基础数据存在

| head 10
| table _time, dest_ip, bytes_out

# 步骤2：检查append后的数据结构

| head 10
| append [| inputlookup MS_IP.csv | eval type="subnet" | head 5]
| table dest_ip, subnet, type

# 步骤3：测试单个子网匹配

| head 10
| eval test_subnet="4.128.0.0/9"
| eval match=if(cidrmatch(test_subnet, dest_ip), 1, 0)
| where match=1
| table dest_ip, test_subnet, match

# 步骤4：使用dedup来检查唯一的dest_ip

| dedup dest_ip
| head 10
| table dest_ip
